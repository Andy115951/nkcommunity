<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.KnowledgeMapper">

    <!-- KnowledgeItem 基础操作 -->
    <insert id="insertKnowledgeItem" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO knowledge_items (user_id, content, category, difficulty)
        VALUES (#{userId}, #{content}, #{category}, #{difficulty})
    </insert>

    <update id="updateKnowledgeItem">
        UPDATE knowledge_items
        SET content = #{content},
            category = #{category},
            difficulty = #{difficulty}
        WHERE item_id = #{itemId}
    </update>
    <delete id="deleteKnowledgeItem">
        DELETE FROM knowledge_items
        WHERE item_id = #{itemId}
    </delete>

    <select id="selectKnowledgeItemById" resultType="com.nowcoder.community.entity.KnowledgeItem">
        SELECT item_id, user_id, content, category, created_at, difficulty
        FROM knowledge_items
        WHERE item_id = #{itemId}
    </select>

    <select id="selectKnowledgeItemsByUser" resultType="com.nowcoder.community.entity.KnowledgeItem">
        SELECT * FROM knowledge_items
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
            LIMIT #{offset}, #{limit}
    </select>

</mapper>